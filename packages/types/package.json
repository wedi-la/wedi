{
  "name": "@wedi/types",
  "version": "0.0.1",
  "description": "Shared TypeScript types and interfaces for Wedi platform",
  "main": "./src/index.ts",
  "types": "./src/index.ts",
  "scripts": {
    "build": "tsc",
    "dev": "tsc --watch",
    "lint": "eslint src/**/*.ts",
    "clean": "rm -rf dist node_modules"
  },
  "devDependencies": {
    "@wedi/typescript-config": "workspace:*",
    "eslint": "^8.57.0",
    "eslint-config-custom": "workspace:*",
    "typescript": "^5.5.4"
  },
  "engines": {
    "node": ">=18.0.0"
  }
} 